#include<iostream>
#include<stdio.h>
#include<algorithm>
#include<string>
#include<bits/stdc++.h>
using namespace std;
int main(){
string s;
cin>>s;
bool flg=false;
for(int i=0;i<s.size();i++){
    if(s[i]=='H'||s[i]=='Q'||s[i]=='9'){
        flg = true;break;
    }
}
if(flg){
    cout<<"YES";
}else{
    cout<<"NO";
}
    
}
