#include<stdio.h>
#include<iostream>
#include<bits/stdc++.h>
using namespace std;
int main(){
int n;
cin >>n;int x=0;
for(int i=0;i<n;i++){
	string s;
	cin>>s;
	for(int j=0;j<s.size();j++){
		if(s.at(j)=='+'){
			x++;j++;
		}else{
			if(s.at(j)=='-'){
				x--;j++;
			}
		}
	}
}
	cout << x;
	
}
